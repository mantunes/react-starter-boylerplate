import React from 'react';
import styles from './styles.scss';
import palette from '../../../styles/_palette.scss';

const Label = () => <div className={`${styles.cenas} ${palette.red}`}>{'Hello world :)'}</div>;

export default Label;

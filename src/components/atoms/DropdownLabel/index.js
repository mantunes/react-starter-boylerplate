import React from 'react';
import styles from './styles.scss';

const DropdownLabel = () =>
  <div>
    <a className={styles.label}>project</a>
    <a className={styles.label}>project</a>
  </div>
export default DropdownLabel;
